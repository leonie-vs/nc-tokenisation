# nc-tokenisation
Implementing a tokeniser using the byte-pair encoding algorithm - explaining how BPE generates a vocabulary of subword tokens

- Dependencies in **requirements.txt**
- Tokeniser class in **tokeniser.py**
- Tests for class in **test_tokeniser.py**, can be performed using Pytest